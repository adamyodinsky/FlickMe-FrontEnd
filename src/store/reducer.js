const initialState = {
  showMovies : false,
  showFilters: false,
  yearValue: [2000, 2019],
  qualityValue: [0, 100]
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
